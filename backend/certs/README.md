Place the AWS DocumentDB CA bundle here as `global-bundle.pem`.

Download example:

```sh
curl -o backend/certs/global-bundle.pem https://truststore.pki.rds.amazonaws.com/global/global-bundle.pem
```
